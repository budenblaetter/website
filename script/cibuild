hostname=$(head -n 1 credentials.txt)
username $(head -n 2 credentials.txt | tail -1)
wwwroot=$(head -n 3 credentials.txt | tail -1)

jekyll build

scp -i deploy_key _site/index.html david@${hostname}:${wwwroot}/index.html

